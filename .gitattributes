# Codex Bar's product is the Swift app; everything else (shell installers,
# the Python config helper, docs) is supporting tooling. Count ONLY Swift
# toward GitHub's language stats so the repo is detected as Swift.
*         linguist-vendored
*.swift   linguist-vendored=false
